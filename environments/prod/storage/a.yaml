# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: AppS3Bucket
metadata:
  name: hiaaa
  namespace: default
  labels:
    app.kubernetes.io/name: hia
    environment: prod
  annotations:
    description: ""
spec:
  # User-facing parameters (same as XRD spec)
  bucketName: hiaaa
  region: us-east-1
  versioning: false
  encryption: false
  publicAccess: false
  tags:
    Name: hiaaa
    Environment: prod
    ManagedBy: crossplane
  # Crossplane v2 - machinery nested under spec.crossplane
  crossplane:
    compositionRef:
      name: s3bucket-composition
