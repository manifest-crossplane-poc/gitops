# S3 Bucket Claim - Crossplane v2 (namespaced XR)
# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: AppS3Bucket
metadata:
  name: testttt
  namespace: default
  labels:
    app.kubernetes.io/name: testttt
    app.kubernetes.io/managed-by: backstage
    backstage.io/kubernetes-id: s3-testttt
    environment: dev
  annotations:
    backstage.io/owner: user:default/betterguy2004
    argocd.argoproj.io/sync-wave: "1"
    description: ""
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: testttt
  region: us-east-1
  versioning: false
  encryption: true
  publicAccess: false
  tags:
    Name: testttt
    Environment: dev
    ManagedBy: crossplane
    CreatedBy: backstage
    Owner: user:default/betterguy2004
  # Crossplane machinery
  crossplane:
    compositionRef:
      name: s3bucket-composition
