
# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: S3Bucket
metadata:
  name: hung-cross
  namespace: default
  labels:
    app.kubernetes.io/name: hung-cross
    environment: dev
  annotations:
    description: ""
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: hung-cross
  region: us-east-1
  versioning: false
  encryption: true
  publicAccess: false
  tags:
    Name: hung-cross
    Environment: dev
    ManagedBy: crossplane  # Crossplane machinery
  compositionRef:
    name: s3bucket-composition