
# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: S3Bucket
metadata:
  name: quanghung
  namespace: default
  labels:
    app.kubernetes.io/name: quanghung
    environment: dev
  annotations:
    description: ""
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: quanghung
  region: us-east-1
  versioning: false
  encryption: false
  publicAccess: true
  tags:
    Name: quanghung
    Environment: dev
    ManagedBy: crossplane  # Crossplane machinery
  compositionRef:
    name: s3bucket-composition