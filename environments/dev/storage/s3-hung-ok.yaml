# S3 Bucket Claim - Crossplane v2 (namespaced XR)
# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: AppS3Bucket
metadata:
  name: hung-ok
  namespace: default
  labels:
    app.kubernetes.io/name: hung-ok
    app.kubernetes.io/managed-by: backstage
    backstage.io/kubernetes-id: s3-hung-ok
    environment: dev
  annotations:
    backstage.io/owner: group:default/platform-team
    argocd.argoproj.io/sync-wave: "1"
    description: "oke"
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: hung-ok
  region: us-east-1
  versioning: false
  encryption: true
  publicAccess: false
  tags:
    Name: hung-ok
    Environment: dev
    ManagedBy: crossplane
    CreatedBy: backstage
    Owner: group:default/platform-team
  # Crossplane machinery
  crossplane:
    compositionRef:
      name: s3bucket-composition
