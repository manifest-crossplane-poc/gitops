
# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: S3Bucket
metadata:
  name: hung-test
  namespace: default
  labels:
    app.kubernetes.io/name: hung-test
    environment: dev
  annotations:
    description: ""
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: hung-test
  region: us-east-1
  versioning: false
  encryption: false
  publicAccess: false
  tags:
    Name: hung-test
    Environment: dev
    ManagedBy: crossplane  # Crossplane machinery
  compositionRef:
    name: s3bucket-composition