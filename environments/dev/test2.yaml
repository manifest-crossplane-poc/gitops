# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: S3Bucket
metadata:
  name: test-fi
  namespace: default
  labels:
    app.kubernetes.io/name: test-fi
    app.kubernetes.io/managed-by: backstage
    backstage.io/kubernetes-id: s3-test-fi
    environment: dev
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    description: ""
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: test-fi
  region: us-east-1
  versioning: false
  encryption: false
  publicAccess: false
  tags:
    Name: test-fi
    Environment: dev
    ManagedBy: crossplane
    CreatedBy: backstage
    Owner: user:default/betterguy2004
  # Crossplane machinery
  compositionRef:
    name: s3bucket-composition
