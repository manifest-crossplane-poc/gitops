# Generated by Backstage at: ${{ "" | now }}
---
apiVersion: storage.example.com/v1alpha1
kind: AppS3Bucket
metadata:
  name: retsetetet
  namespace: default
  labels:
    app.kubernetes.io/name: retsetetet
    app.kubernetes.io/managed-by: backstage
    backstage.io/kubernetes-id: s3-retsetetet
    environment: dev
  annotations:
    backstage.io/owner: group:default/platform-team
    argocd.argoproj.io/sync-wave: "1"
    description: "bar"
spec:
  # Crossplane v2 - flat spec structure (no nested parameters)
  bucketName: retsetetet
  region: us-east-1
  versioning: false
  encryption: true
  publicAccess: false
  tags:
    Name: retsetetet
    Environment: dev
    ManagedBy: crossplane
    CreatedBy: backstage
    Owner: group:default/platform-team
  # Crossplane machinery
  compositionRef:
    name: s3bucket-composition
